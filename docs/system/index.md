# System documentation

Current specifications describe accepted behaviour on the main line, one file
per capability in `capabilities/<capability>.md`, named from the glossary and
written from the skills' capability template.

## Coverage

No coverage yet: no behaviour has been accepted. The first behaviour change
adds its capability against an absent baseline, and the document gate replays
it into `capabilities/` at closure.

## Known unknowns

Everything accepted: the first product code (the change graph-foundation) is
in progress and describes its behaviour in `docs/changes/graph-foundation/`
until it is accepted.
